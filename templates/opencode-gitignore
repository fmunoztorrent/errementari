# Errementari — local artifacts (per machine, never commit)
node_modules
package-lock.json
bun.lock

# Pipeline runtime state (session-local, regenerated automatically)
pipeline/state.json
pipeline/close-pending.json
pipeline/coordination.json
